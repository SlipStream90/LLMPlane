# Frontend environment variables for Vercel deployment.
# Set these in Vercel Dashboard → Settings → Environment Variables.
#
# These values come from your Railway backend deployment.
# Get the Railway URL from: Railway Dashboard → your service → Settings → Networking → Public Network

# ── Required ──────────────────────────────────────────────────
# The backend mounts all v1 routes under /api/v1. Either form works — the
# frontend appends /api/v1 when it is missing:
#   https://<your-railway-app-name>.up.railway.app
#   https://<your-railway-app-name>.up.railway.app/api/v1
NEXT_PUBLIC_API_URL=

# WebSocket origin, same host with a wss:// scheme and NO /api/v1 suffix.
# Format: wss://<your-railway-app-name>.up.railway.app
NEXT_PUBLIC_WS_URL=

# ── Optional ──────────────────────────────────────────────────
# Grafana Cloud: https://grafana.com → Your Stack URL
NEXT_PUBLIC_GRAFANA_URL=

# Langfuse Cloud: https://cloud.langfuse.com
NEXT_PUBLIC_LANGFUSE_URL=https://cloud.langfuse.com
