# Backend (control plane) runtime dependencies.
# Versions verified against PyPI on 2026-08-01 (methodology_brief.md Article IV:
# pin at implementation time, never from recall).
#
# NOTE: `litellm` is deliberately absent. Per ADR-001 the gateway is a sibling
# container running the official LiteLLM Proxy image; `backend` configures it
# over HTTP and never imports it as a routing library.

fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic-settings==2.14.2
python-multipart==0.0.32

# Database
SQLAlchemy==2.0.51
asyncpg==0.31.0
alembic==1.18.5
greenlet==3.5.4

# Redis: cache, WebSocket pub/sub, Celery broker handle, Streams consumer (ADR-004)
redis==8.1.0

# Security
argon2-cffi==25.1.0
cryptography==50.0.0

# Outbound HTTP: gateway, provider health checks, Langfuse trace proxy
httpx==0.28.1

# Task enqueue only — task bodies live in workers/
celery==5.6.3

# Observability (OTel GenAI semantic conventions, ARCHITECTURE.md 4.3)
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
prometheus-fastapi-instrumentator==8.1.0

# Renders the gateway's LiteLLM router config (RoutingConfigService)
PyYAML==6.0.3

orjson==3.11.9
